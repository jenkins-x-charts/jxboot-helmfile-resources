buildPacks: {}

#  "classic-workloads":
#    label: "Library Workloads: CI+Release but no CD"
#    gitUrl: "https://github.com/jenkins-x-buildpacks/jenkins-x-classic.git"
#    gitRef: "master"

chartmuseumServiceLink:
  enabled: false

  # specify the external name here for where the chartmuseum should be
  externalname: ""

kaniko:
  # lets you specify additional kaniko flags such as "--insecure" for insecure registries etc
  flags: ""

schedulers:
  # to enable/disable the default scheduler
  default: true

lighthouseEngine: tekton


basicAuthSecrets:
  enabled: true
  
pipeline:
  rbac:
    cluster: true
  serviceAccount:
    name: "tekton-bot"
    annotations: {}

  auth:
    git:
      username: "myuser"
      password: "mypwd"
      url: https://github.com
    docker:
      # if specified use the docker config.json style secret like this:
      # https://github.com/tektoncd/pipeline/blob/master/docs/auth.md#configuring-docker-authentication-for-docker
      configJson: "{}"

      # otherwise use basic auth style secret
      username:
      password:
      url: https://index.docker.io/v1/
      # url: https://gcr.io
      host: docker.io
      email: jenkins-x@googlecloud.com

bucketrepo:
  ingress:
    tls:
      secretName: ""

chartmuseum:
  ingress:
    tls:
      secretName: ""

docker-registry:
  enabled: false
  ingress:
    tls:
      secretName: ""

hook:
  ingress:
    tls:
      secretName: ""

nexus:
  ingress:
    tls:
      secretName: ""

# TODO we want to remove exposecontroller at some point
exposer: Ingress

extensions:
  enabled: true

gcactivities:
  serviceaccount:
    enabled: true
  cronjob:
    enabled: true
    schedule: "0/30 */3 * * *"
  args:
  - "gc"
  - "activities"
  - "--batch-mode"
  image:
    repository: gcr.io/jenkinsxio-labs/jxl
    tag: 0.0.127
  role:
    enabled: true
    rules:
    - apiGroups:
      - jenkins.io
      resources:
      - pipelineactivities
      verbs:
      - list
      - delete
    - apiGroups:
      - jenkins.io
      resources:
      - environments
      verbs:
      - get
      - list
      - patch
      - update
      - watch
    - apiGroups:
      - jenkins.io
      resources:
      - plugins
      verbs:
      - get
      - list
      - watch
    - apiGroups:
      - ""
      resources:
      - secrets
      - services
      verbs:
      - get
    - apiGroups:
      - apps
      resources:
      - deployments
      verbs:
      - get
    - apiGroups:
      - tekton.dev
      resources:
      - pipelineruns
      verbs:
      - get
      - list
      - watch
      - delete
  clusterrole:
    enabled: true
    rules:
    - apiGroups:
      - apiextensions.k8s.io
      resources:
      - customresourcedefinitions
      verbs:
      - get
      - create
      - patch
      - update
    - apiGroups:
      - ""
      resources:
      - namespaces
      verbs:
      - get
      - delete
      - list
    - apiGroups:
      - apps
      resources:
      - deployments
      verbs:
      - get

gcpods:
  serviceaccount:
    enabled: true
  cronjob:
    enabled: true
    schedule: "0/30 */3 * * *"
  args:
  - "gc"
  - "pods"
  - "--batch-mode"
  image:
    repository: gcr.io/jenkinsxio-labs/jxl
    tag: 0.0.127
  role:
    enabled: true
    rules:
    - apiGroups:
      - jenkins.io
      resources:
      - environments
      - plugins
      verbs:
      - get
      - list
      - watch
    - apiGroups:
      - ""
      resources:
      - pods
      verbs:
      - get
      - delete
      - list

podTemplates:
  enabled: true
  agent:
    dockerHostPath: ""
    dockerMountPath: ""
    dockerConfig: false
  templates:
    aws-cdk:
      name: aws-cdk
      label: jenkins-aws-cdk
      devPodPorts: 9229, 3000, 8080, 5005
      volumes:
        - type: Secret
          secretName: jenkins-maven-settings
          mountPath: /root/.m2/
        - type: Secret
          secretName: jenkins-release-gpg
          mountPath: /home/jenkins/.gnupg
      envVars:
        _JAVA_OPTIONS: '-XX:+UnlockExperimentalVMOptions -Dsun.zip.disableMemoryMapping=true -XX:+UseParallelGC -XX:MinHeapFreeRatio=5 -XX:MaxHeapFreeRatio=10 -XX:GCTimeRatio=4 -XX:AdaptiveSizePolicyWeight=90 -Xms10m -Xmx512m'
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      containers:
        aws-cdk:
          image: gcr.io/jenkinsxio/builder-maven-nodejs:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "2"
          limitMemory: "2048Mi"
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Maven:
      name: maven
      label: jenkins-maven
      devPodPorts: 5005, 8080
      # annotations:
      #   iam.amazonaws.com/role: arn:aws:iam::accountid:role/rolename
      volumes:
      - type: Secret
        secretName: jenkins-maven-settings
        mountPath: /root/.m2/
      - type: Secret
        secretName: jenkins-release-gpg
        mountPath: /home/jenkins/.gnupg
      envVars:
        _JAVA_OPTIONS: '-XX:+UnlockExperimentalVMOptions -XX:+UseCGroupMemoryLimitForHeap -Dsun.zip.disableMemoryMapping=true -XX:+UseParallelGC -XX:MinHeapFreeRatio=5 -XX:MaxHeapFreeRatio=10 -XX:GCTimeRatio=4 -XX:AdaptiveSizePolicyWeight=90 -Xms10m -Xmx192m'
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
        # lets disable the noisy download artifact logging messages
        MAVEN_OPTS: -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=warn

      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Maven:
          image: gcr.io/jenkinsxio/builder-maven:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "1"
          limitMemory: "1024Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Maven-Java11:
      name: maven-java11
      label: jenkins-maven-java11
      devPodPorts: 5005, 8080
      volumes:
      - type: Secret
        secretName: jenkins-maven-settings
        mountPath: /root/.m2/
      - type: Secret
        secretName: jenkins-release-gpg
        mountPath: /home/jenkins/.gnupg
      envVars:
        _JAVA_OPTIONS: '-XX:+UnlockExperimentalVMOptions -Dsun.zip.disableMemoryMapping=true -XX:+UseParallelGC -XX:MinHeapFreeRatio=5 -XX:MaxHeapFreeRatio=10 -XX:GCTimeRatio=4 -XX:AdaptiveSizePolicyWeight=90 -Xms10m -Xmx192m'
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins

        # lets disable the noisy download artifact logging messages
        MAVEN_OPTS: -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=warn
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Maven:
          image: gcr.io/jenkinsxio/builder-maven-java11:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "1"
          limitMemory: "1024Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Maven-GraalVM:
      name: maven-graalvm
      label: jenkins-maven-graalvm
      devPodPorts: 5005, 8080
      volumes:
      - type: Secret
        secretName: jenkins-maven-settings
        mountPath: /root/.m2/
      - type: Secret
        secretName: jenkins-release-gpg
        mountPath: /home/jenkins/.gnupg
      envVars:
        _JAVA_OPTIONS: '-XX:+UnlockExperimentalVMOptions -Dsun.zip.disableMemoryMapping=true -XX:+UseParallelGC -XX:MinHeapFreeRatio=5 -XX:MaxHeapFreeRatio=10 -XX:GCTimeRatio=4 -XX:AdaptiveSizePolicyWeight=90 -Xms10m -Xmx1800m'
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins

        # lets disable the noisy download artifact logging messages
        MAVEN_OPTS: -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=warn
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Maven:
          image: gcr.io/jenkinsxio/builder-maven-graalvm:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "2Gi"
          limitCpu: "1"
          limitMemory: "3Gi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Gradle:
      name: gradle
      label: jenkins-gradle
      devPodPorts: 5005, 8080
      volumes:
      - type: Secret
        secretName: jenkins-release-gpg
        mountPath: /home/jenkins/.gnupg
      envVars:
        _JAVA_OPTIONS: '-XX:+UnlockExperimentalVMOptions -XX:+UseCGroupMemoryLimitForHeap -Dsun.zip.disableMemoryMapping=true -XX:+UseParallelGC -XX:MinHeapFreeRatio=5 -XX:MaxHeapFreeRatio=10 -XX:GCTimeRatio=4 -XX:AdaptiveSizePolicyWeight=90 -Xms10m -Xmx192m'
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Gradle:
          image: gcr.io/jenkinsxio/builder-gradle:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "1"
          limitMemory: "1024Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Scala:
      name: scala
      label: jenkins-scala
      devPodPorts: 5005, 8080
      volumes:
      - type: Secret
        secretName: jenkins-maven-settings
        mountPath: /root/.m2/
      - type: Secret
        secretName: jenkins-release-gpg
        mountPath: /home/jenkins/.gnupg
      envVars:
        _JAVA_OPTIONS: '-XX:+UnlockExperimentalVMOptions -XX:+UseCGroupMemoryLimitForHeap -Dsun.zip.disableMemoryMapping=true -XX:+UseParallelGC -XX:MinHeapFreeRatio=5 -XX:MaxHeapFreeRatio=10 -XX:GCTimeRatio=4 -XX:AdaptiveSizePolicyWeight=90 -Xms10m -Xmx192m'
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Scala:
          image: gcr.io/jenkinsxio/builder-scala:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "1"
          limitMemory: "1024Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Go:
      name: go
      label: jenkins-go
      devPodPorts: 2345, 8080
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Go:
          image: gcr.io/jenkinsxio/builder-go:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "600Mi"
          limitCpu: "1"
          limitMemory: "1448Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Terraform:
      name: terraform
      label: jenkins-terraform
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Terraform:
          image: gcr.io/jenkinsxio/builder-terraform:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "600Mi"
          limitCpu: "1"
          limitMemory: "1448Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Rust:
      name: rust
      label: jenkins-rust
      devPodPorts: 8080
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Rust:
          image: gcr.io/jenkinsxio/builder-rust:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "1"
          limitMemory: "1024Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Newman:
      name: newman
      label: jenkins-newman
      devPodPorts: 9229, 3000
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Newman:
          image: gcr.io/jenkinsxio/builder-newman:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "1"
          limitMemory: "1024Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Nodejs8x:
      name: nodejs8x
      label: jenkins-nodejs8x
      devPodPorts: 9229, 3000, 8080
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Nodejs:
          image: gcr.io/jenkinsxio/builder-nodejs8x:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "2"
          limitMemory: "2048Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Nodejs10x:
      name: nodejs10x
      label: jenkins-nodejs10x
      devPodPorts: 9229, 3000, 8080
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Nodejs:
          image: gcr.io/jenkinsxio/builder-nodejs10x:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "2"
          limitMemory: "2048Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Nodejs12x:
      name: nodejs12x
      label: jenkins-nodejs12x
      devPodPorts: 9229, 3000, 8080
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      containers:
        Nodejs:
          image: gcr.io/jenkinsxio/builder-nodejs12x:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "2"
          limitMemory: "2048Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    php5x:
      name: php5x
      label: jenkins-php5x
      devPodPorts: 9229, 3000, 8080
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      containers:
        Nodejs:
          image: gcr.io/jenkinsxio/builder-php5x:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "2"
          limitMemory: "2048Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    php7x:
      name: php7x
      label: jenkins-php7x
      devPodPorts: 9229, 3000, 8080
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      containers:
        Nodejs:
          image: gcr.io/jenkinsxio/builder-php7x:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "2"
          limitMemory: "2048Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Nodejs:
      name: nodejs
      label: jenkins-nodejs
      devPodPorts: 9229, 3000, 8080
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Nodejs:
          image: gcr.io/jenkinsxio/builder-nodejs:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "2"
          limitMemory: "2048Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Maven-Nodejs:
      name: maven-nodejs
      label: jenkins-maven-nodejs
      devPodPorts: 9229, 3000, 8080, 5005
      volumes:
        - type: Secret
          secretName: jenkins-maven-settings
          mountPath: /root/.m2/
        - type: Secret
          secretName: jenkins-release-gpg
          mountPath: /home/jenkins/.gnupg
      envVars:
        _JAVA_OPTIONS: '-XX:+UnlockExperimentalVMOptions -Dsun.zip.disableMemoryMapping=true -XX:+UseParallelGC -XX:MinHeapFreeRatio=5 -XX:MaxHeapFreeRatio=10 -XX:GCTimeRatio=4 -XX:AdaptiveSizePolicyWeight=90 -Xms10m -Xmx192m'
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      containers:
        MavenNodejs:
          image: gcr.io/jenkinsxio/builder-maven-nodejs:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "2"
          limitMemory: "2048Mi"
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    JX-base:
      name: jx-base
      label: jenkins-jx-base
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        JX-base:
          image: gcr.io/jenkinsxio/builder-jx:2.1.150-770
          privileged: true
          requestCpu: "200m"
          requestMemory: "256Mi"
          limitCpu: "400m"
          limitMemory: "512Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Promote:
      name: promote
      label: jenkins-promote
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Promote:
          image: gcr.io/jenkinsxio/builder-jx:2.1.150-770
          privileged: true
          requestCpu: "200m"
          requestMemory: "100Mi"
          limitCpu: "400m"
          limitMemory: "200Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Packer:
      name: packer
      label: jenkins-packer
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Packer:
          image: hashicorp/packer:1.4.1
          requestCpu: "100m"
          requestMemory: "128Mi"
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Python2:
      name: python2
      label: jenkins-python2
      devPodPorts: 3000, 8000
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Python:
          image: gcr.io/jenkinsxio/builder-python2:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "2"
          limitMemory: "2048Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Python:
      name: python
      label: jenkins-python
      devPodPorts: 3000, 8000
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Python:
          image: gcr.io/jenkinsxio/builder-python:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "2"
          limitMemory: "2048Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Python37:
      name: python37
      label: jenkins-python37
      devPodPorts: 3000, 8000
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Python:
          image: gcr.io/jenkinsxio/builder-python37:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "2"
          limitMemory: "2048Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Ruby:
      name: ruby
      label: jenkins-ruby
      devPodPorts: 1234, 3000
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Ruby:
          image: gcr.io/jenkinsxio/builder-ruby:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "2"
          limitMemory: "2048Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Swift:
      name: swift
      label: jenkins-swift
      devPodPorts: 8080
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Swift:
          image: gcr.io/jenkinsxio/builder-swift:2.1.150-770
          privileged: true
          requestCpu: "400m"
          requestMemory: "512Mi"
          limitCpu: "2"
          limitMemory: "2048Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Dlang:
      name: dlang
      label: jenkins-dlang
      devPodPorts: 8080
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Dlang:
          image: gcr.io/jenkinsxio/builder-dlang:2.1.150-770
          privileged: true
          requestCpu: "500m"
          requestMemory: "1024Mi"
          limitCpu: "1"
          limitMemory: "2048Mi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/sh -c"
          args: "cat"
          tty: true
    Machine-Learning:
      name: machine-learning
      label: jenkins-machine-learning
      devPodPorts: 3000, 8000
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Machine-Learning:
          image: gcr.io/jenkinsxio/builder-machine-learning:2.1.150-770
          privileged: true
          requestCpu: "2000m"
          requestMemory: "4Gi"
          limitCpu: "8000m"
          limitMemory: "16Gi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/bash -cl"
          args: "cat"
          tty: true
    Machine-Learning-Gpu:
      name: machine-learning-gpu
      label: jenkins-machine-learning-gpu
      devPodPorts: 3000, 8000
      envVars:
        GIT_COMMITTER_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_EMAIL: jenkins-x@googlegroups.com
        GIT_AUTHOR_NAME: jenkins-x-bot
        GIT_COMMITTER_NAME: jenkins-x-bot
        XDG_CONFIG_HOME: /home/jenkins
      serviceAccount: tekton-sa
      # Node labels and tolerations for pod assignment
      # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#nodeselector
      # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
      # nodeSelector:
      #   dedicated: jenkins-x-builder
      # tolerations:
      # - effect: NoSchedule
      #   key: dedicated
      #   operator: Equal
      #   value: jenkins-x-builder
      containers:
        Machine-Learning:
          image: gcr.io/jenkinsxio/builder-machine-learning-gpu:2.1.150-770
          privileged: true
          requestCpu: "1000m"
          requestMemory: "1Gi"
          limitCpu: "2000m"
          limitMemory: "2Gi"
          # You may want to change this to true while testing a new image
          # AlwaysPullimage: true
          command: "/bin/bash -cl"
          args: "cat"
          tty: true
  Persistence:
    Enabled: true
    ## A manually managed Persistent Volume and Claim
    ## Requires Persistence.Enabled: true
    ## If defined, PVC must be created manually before volume will be bound
    # ExistingClaim:

    ## jenkins data Persistent Volume Storage Class
    ## If defined, storageClassname: <storageClass>
    ## If set to "-", storageClassname: "", which disables dynamic provisioning
    ## If undefined (the default) or set to null, no storageClassName spec is
    ##   set, choosing the default provisioner.  (gp2 on AWS, standard on
    ##   GKE, AWS & OpenStack)
    ##
    # StorageClass: "-"

    AccessMode: ReadWriteOnce
    Size: 30Gi
    volumes:
    #  - name: nothing
    #    emptyDir: {}
    mounts:
  #  - mountPath: /var/nothing
  #    name: nothing
  #    readOnly: true

  NetworkPolicy:
    # Enable creation of NetworkPolicy resources.
    Enabled: false
    # For Kubernetes v1.4, v1.5 and v1.6, use 'extensions/v1beta1'
    # For Kubernetes v1.7, use 'networking.k8s.io/v1'
    ApiVersion: extensions/v1beta1

  ## Install Default RBAC roles and bindings
  rbac:
    install: true
    serviceAccountname: jenkins
    # RBAC api version (currently either v1beta1 or v1alpha1)
    apiVersion: v1beta1
    # Cluster role reference
    roleRef: cluster-admin

ingress:
  annotations:
#      kubernetes.io/ingress.class: nginx
  apiVersion: networking.k8s.io/v1beta1

  # on old clusters use:
  #apiVersion: {{ .Values.ingress.apiVersion | default "networking.k8s.io/v1beta1" }}

  # define the ingress prefixes for the different services
  prefix:
    bucketrepo: bucketrepo
    chartmuseum: chartmuseum
    hook: hook
    nexus: nexus
    dockerRegistry: docker-registry

  # allows you to specify custom hosts
  customHosts:
    bucketrepo: ""
    chartmuseum: ""
    hook: ""
    nexus: ""
    dockerRegistry: ""

  # allows you to specify custom ingress class
  customIngressClass:
    bucketrepo: ""
    chartmuseum: ""
    hook: ""
    nexus: ""
    dockerRegistry: ""

nexusServiceLink:
  enabled: false

  # specify the external name here for where the nexus should point
  externalname: ""

teamRoles:
  viewer:
    enabled: true
  team-admin:
    enabled: true

versions:
  # TODO update with latest builders version
  builders: latest

  # TODO update with latest builders version
  jxl: 0.0.94

# standard YAML files for jx boot:
secrets:
  adminUser:
    username: "admin"
    password: ""
  chartMuseum:
    username: ""
    password: ""
  featureFlagAPIKey: ""
  dockerConfig: "{}"
  gpg:
    pubring: ""
    secjenkins: ""
    secring: ""
    trustdb: ""
  kanikoSecret: ""
  mavenSettings: ""
  npmToken: ""
  pipelineUser:
    username: ""
    email: ""
    token: ""
  tektonBucket: ""

jxRequirements:
  autoUpdate:
    enabled: false
    schedule: ""
  bootConfigURL: https://github.com/jenkins-x/jxr-boot-config.git
  buildPacks:
    buildPackLibrary:
      gitRef: master
      gitURL: https://github.com/jenkins-x/jxr-packs-kubernetes.git
  cluster:
    clustername: ""
    devEnvApprovers:
      - someone
    environmentGitOwner: ""
    environmentGitPublic: false
    gitKind: github
    gitName: github
    gitPublic: false
    gitServer: https://github.com
    namespace: jx
    project: ""
    provider: "kubernetes"
    zone: ""
  deployOptions:
    canary: false
    hpa: false
  environments:
    - key: dev
      repository: environment-mycluster-dev
  gitops: true
  ingress:
    domain: ""
    externalDNS: false
    kind: ""
    namespaceSubDomain: -jx.
    serviceType: ""
    tls:
      email: ""
      enabled: false
      production: false
      secretName: ""
  pipelineUser:
    username: ""
    email: ""
  repository: nexus
  secretStorage: local
  storage:
    backup:
      enabled: false
      url: ""
    logs:
      enabled: false
      url: ""
    reports:
      enabled: false
      url: ""
    repository:
      enabled: false
      url: ""
  versionStream:
    ref: master
    url: https://github.com/jenkins-x/jenkins-x-versions.git
  webhook: lighthouse
